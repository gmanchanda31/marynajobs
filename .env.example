# Neon Postgres — auto-provisioned by Vercel Marketplace integration in production.
# For local dev, paste a Neon connection string (free tier branch).
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Password for Maryna to access the tracker UI. Set anything you like.
APP_PASSWORD=change-me

# Bearer token the local AI bot uses to POST/PATCH jobs via /api/jobs.
BOT_TOKEN=change-me-too
